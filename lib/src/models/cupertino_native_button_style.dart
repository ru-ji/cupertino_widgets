/// Styles for the native iOS button
enum CupertinoNativeButtonStyle {
  automatic,
  filled,
  tinted,
  plain,
  glass,
  glassProminent,
}

/// Control sizes for the native button
enum CupertinoNativeControlSize { mini, small, regular, large, extraLarge }
